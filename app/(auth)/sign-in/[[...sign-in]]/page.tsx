/**
 * Renders the sign-in page component.
 * @returns JSX element that represents the sign-in page.
 */
import { SignIn } from "@clerk/nextjs";
 
export default function Page() {
  return <SignIn />;
}